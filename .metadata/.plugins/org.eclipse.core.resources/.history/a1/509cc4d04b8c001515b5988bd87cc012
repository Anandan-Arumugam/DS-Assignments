package in.edu.ssn.sase.FCS.hw09;

import java.util.Comparator;
class ElementComparator<T extends Comparable<T>> implements Comparator<T> {
	  public int compare(T a, T b) {
	    return a.compareTo(b);
	  }
	}
